---
import AdminLayout from "../../layouts/AdminLayout.astro";
import { verifyToken } from "../../lib/server/auth";
import AdminAnalysisList from "../../components/react/AdminAnalysisList";

const token = Astro.cookies.get("admin_token")?.value;

if (!token || !verifyToken(token)) {
    return Astro.redirect("/admin/login");
}
---

<AdminLayout title="Análisis de Proyectos">
    <div class="mb-8 flex justify-between items-center">
        <div>
            <h1
                class="text-3xl font-bold leading-tight text-white tracking-tight"
            >
                Análisis de Proyectos
            </h1>
            <p class="text-gray-400 mt-2">
                Solicitudes de estudio técnico y económico.
            </p>
        </div>
        <a
            href="/admin"
            class="text-gray-400 hover:text-white transition-colors flex items-center gap-2"
        >
            <svg
                xmlns="http://www.w3.org/2000/svg"
                class="h-5 w-5"
                viewBox="0 0 20 20"
                fill="currentColor"
            >
                <path
                    fill-rule="evenodd"
                    d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z"
                    clip-rule="evenodd"></path>
            </svg>
            Volver
        </a>
    </div>

    <AdminAnalysisList client:only="react" />
</AdminLayout>
